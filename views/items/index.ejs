<%- include('../partials/header') %>
    <h1>What do you want to buy</h1>
    <% if(user) {%> 
    <h1><%= user.isSeller %> </h1>
    <h1><%= user.googleId %> </h1>
    <h4><% user.item.forEach( i => { %>
        <h3><%= i %> </h3>
        <% }) %>
    </h4>
    <% } %> 

<%- include('../partials/footer') %>